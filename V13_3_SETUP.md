# Tiger Chat v13.3 Setup

1. Run `supabase/v13_3_home_focus_school.sql` in Supabase SQL Editor.
2. Run `npm run build`.
3. Test `/home`, `/chat`, `/labs`, Focus Mode, school rotation, and push notifications.
4. Commit/push only after the build passes.
