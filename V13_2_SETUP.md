# Tiger Chat v13.2

1. Run `supabase/v13_2_staff_roles.sql` in Supabase SQL Editor.
2. Run `npm run build`.
3. Test iPad/tablet layout, `/customize`, `/chat`, and `/moderation`.
4. The first existing app admin is promoted to Owner, preferring an account whose admin tag is `OWNER`.
